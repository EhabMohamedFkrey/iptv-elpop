{
  "name": "iptv-elpop",
  "version": "1.0.0",
  "description": "IPTV Elpop Android App",
  "scripts": {
    "build": "echo 'No build step needed for static files'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/cli": "^6.0.0",
    "@capacitor-community/keep-awake": "^5.0.0",
    "cordova-plugin-streaming-media": "^2.3.0"
  },
  "devDependencies": {
  }
}
